#!/bin/sh

# Copy files from $HOME into here

FILES=(
  .gitconfig
  .gitignore
  .tmux.conf
  .vimrc
  .zsh_aliases
  .zshrc
  .config/starship.toml
  .config/gh
  .config/helix
  .config/iterm2
  .config/nvim
  .config/ranger
  .config/raycast
  .config/tmuxinator
  .config/zed
)
