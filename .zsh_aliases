# Helpers
alias be='bundle exec'

# Utils
alias cat='bat'
alias ls='exa'
alias tf='terraform'

# Paths
alias hosts='/private/etc/hosts'

# Personal
alias notes='code ~/dev/cyborg-brain/'
alias bench='hyperfine'

# Dotfiles
alias updots='sh ~/dev/dotfiles/update.sh'

# Git
alias cor="git for-each-ref --sort=-committerdate --format='%(refname:short)' refs/heads | fzf --reverse --height 35% --nth 1 | awk '{print $1}' | xargs git checkout"

